#! /bin/sh

for file in $(find . -name 'test*.rkt')
do
  echo "testing $file"
  racket $file || { echo ":(" ; exit 1;}
  echo $?
  if [ -z $? ]
  then
      echo ":("
      exit $?
  fi
done

echo ":)"
